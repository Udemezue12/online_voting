# from apscheduler.schedulers.background import BackgroundScheduler as Scheduler
# from online import create_app
# from online.vote.routes import update_election_status

# app = create_app()

# scheduler = Scheduler()
# scheduler.add_job(func=update_election_status, args=[app], trigger="interval", minutes=1)  # Pass app as argument
# scheduler.start()
